<div class="container">

  <div>
    <%= link_to "Add a new article", new_article_path, class:'add-new' %>
  </div>

  <div>
    <ul>
      <% @articles.each do |article| %>
        <li><%= link_to article.title, article_path(article) %></li>
      <% end %>
    </ul>
  </div>

</div>
